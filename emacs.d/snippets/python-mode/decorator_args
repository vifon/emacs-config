# -*- mode: snippet -*-
# name: decorator with args
# key: deco
# --
def ${1:name}(${2:*args, **kwargs}):
    def decorator(f):
        @functools.wraps(f)
        def wrapper(*args, **kwargs):
            return f(*args, **kwargs)
        return wrapper
    return decorator